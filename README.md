# Autonomous Coding Agent Benchmark (Python)

This repository contains a small Python project used to benchmark autonomous AI coding agents
against conventional LLM code-generation approaches on software maintenance tasks
(bug fixing, feature implementation, and unit-test generation).

## How to run tests

1. Activate the virtual environment.
2. Run: `python -m unittest discover -s tests -p "test_*.py"`